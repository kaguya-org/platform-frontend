import styled from 'styled-components';

export const Container = styled.div`
  user-select: none;

  position: fixed;
  z-index: 2;
  top: 0;
  right: 0;
  padding: 30px 10px;
  overflow: hidden;
`;
